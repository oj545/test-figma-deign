import React from "react";
import TarotCard from "../UI/TarotCard";
import heroCard from "../assets/heroCard.jpg";
function HeroSection() {
  return (
    <div>
      {/* <TarotCard image={heroCard} /> */}
      <TarotCard image={heroCard} />
      sdf
    </div>
  );
}

export default HeroSection;
